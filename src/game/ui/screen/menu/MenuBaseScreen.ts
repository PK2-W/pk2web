import { PkUIComponentImpl } from '@ng/ui/PkUIComponentImpl';
import { UIWaveText } from '../../component/UIWaveText';

export abstract class MenuBaseScreen {
    private _menu = {
        main: {},
        controls: {},
        graphics: {},
        audio: {}
    };
}
