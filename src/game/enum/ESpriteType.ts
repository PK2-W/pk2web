export enum ESpriteType { //Type
    /** No type. Use NULL instead. */
    TYYPPI_EI_MIKAAN,
    /** Game character. */
    TYYPPI_PELIHAHMO,
    /** Bonus. */
    TYYPPI_BONUS,
    /** Ammunition. */
    TYYPPI_AMMUS,
    /** Teleport. */
    TYYPPI_TELEPORTTI,
    /** Background. */
    TYYPPI_TAUSTA
}