import { PkUIComponent } from '@ng/screen/PkUIComponent';
import { UIWaveText } from '../../ui/UIWaveText';

export abstract class MenuBaseScreen {
    private _menu = {
        main: {},
        controls: {},
        graphics: {},
        audio: {}
    };
}
