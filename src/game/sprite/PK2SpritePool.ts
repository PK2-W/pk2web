import { Sprite } from '@game/sprite/Sprite';
import { MAX_SPRITES } from '../../support/constants';
import { CVect, cvect } from '../../support/types';

export class PK2SpritePool {
    private _pool: CVect<Sprite> = cvect(MAX_SPRITES);
    
    public
}
